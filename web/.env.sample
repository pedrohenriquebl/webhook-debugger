# Sample environment for the frontend (Vite)
# Copy to `.env` or `.env.local` if you need to override values in development

# Base URL of the API used by the frontend (change if API runs in a different host/port)
VITE_API_URL=http://localhost:3333

# Example: if you proxy or use a different path, update accordingly
# VITE_API_URL=https://api.example.com
