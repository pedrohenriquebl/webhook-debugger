# Workspace .env sample
# Optional top-level environment sample. Projects have their own .env.sample files in `api/` and `web/`.

# You can put shared variables here if needed, or copy to individual package .env files.
